apiVersion: v1
kind: Service
metadata:
  name: final-project
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: final-project
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP