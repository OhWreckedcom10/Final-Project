apiVersion: v1
kind: Service
metadata:
  name: final-project-service
spec:
  type: NodePort
  selector:
    app: final-project
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30080